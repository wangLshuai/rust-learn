use std::io::Result as IoResult;
use std::{collections::HashMap, fmt::Result};

fn main() {
    let mut map = HashMap::new();
    map.insert(1, 2);
}

// fn function1() -> fmt::Result {
//     return fmt::Result<i32,i32>::Err;
// }

// fn function2() -> io::Result<()> {}
